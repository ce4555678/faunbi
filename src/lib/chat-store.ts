// lib/chat-store.ts
import { UIMessage } from "ai"

// lib/chat-store.ts
export async function saveChat({
  chatId,
  userId,
  messages,
}: {
  chatId: string
  userId: string
  messages: UIMessage[]
}) {}
